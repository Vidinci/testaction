apiVersion: v1
kind: Service
metadata:
  name: {{ include "realworldchart.fullname" . }}
  labels:
    {{- include "realworldchart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "realworldchart.selectorLabels" . | nindent 4 }}
